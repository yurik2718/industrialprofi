<% meta_title(t("journal.edit_title")) %>

<section class="section">
  <div class="container container--reading journal">
    <h1 class="section-title journal__intro"><%= t("journal.edit_title") %></h1>
    <%= render "form", entry: @entry %>
  </div>
</section>
