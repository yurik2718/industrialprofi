<%# locals: (entry:) -%>
<%= form_with model: entry, class: "journal-form" do |form| %>
  <% if entry.errors.any? %>
    <div class="auth__errors" role="alert">
      <ul>
        <% entry.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="journal-form__field">
    <%= form.label :title, t("journal.field_title") %>
    <%= form.text_field :title, class: "input", autofocus: true %>
  </div>

  <div class="journal-form__field">
    <%= form.label :lesson_id, t("journal.field_lesson") %>
    <%# Practice lessons only: the journal logs a practice task done, not theory —
        keeps this picker short instead of listing every lesson of every profession. %>
    <%= form.grouped_collection_select :lesson_id,
          Path.published.localized.ordered.with_practice_lessons, :practice_lessons, :title, :id, :title,
          { include_blank: t("journal.lesson_none") }, { class: "input" } %>
  </div>

  <div class="journal-form__field">
    <%= form.label :body, t("journal.field_body") %>
    <p class="journal-form__hint"><%= t("journal.body_hint") %></p>
    <%# Journal is text-only by design (no uploads on the SQLite disk) — disable
        Lexxy attachments so there's no broken image button. Lessons keep theirs. %>
    <%= form.rich_text_area :body, class: "lexxy-content", attachments: "false" %>
  </div>

  <div class="journal-form__actions">
    <%= form.submit t("journal.save"), class: "btn btn--reversed btn--large" %>
    <%= link_to t("admin.cancel"), journal_entries_path, class: "btn btn--large" %>
  </div>
<% end %>
