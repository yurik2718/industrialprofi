<%# locals: (token:) -%>
<%# The unified topic badge: the line icon centred in its disc — one quiet house
    style for every course/path circle. The framing circle comes from the
    context (progress ring, bordered disc, hero circle). %>
<span class="topic-badge">
  <%= topic_icon_svg(token) %>
</span>
