module Admin
  # The author's reference: what each resource type and text callout means and
  # when to use it. Read-only, open to every editor (no admin gate). The page
  # renders the REAL badges and callouts, so it can never drift from production.
  class GuideController < BaseController
    def show; end
  end
end
